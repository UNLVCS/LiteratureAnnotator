[project]
name = "literature-annotator-utilities"
version = "0.1.0"
description = "Queue and vector DB utility scripts."
requires-python = ">=3.10"
dependencies = [
  "pinecone>=3.0.0",
  "pydantic>=2.0.0",
  "pyyaml>=6.0",
  "redis>=5.0.0",
]

[tool.uv]
package = false
